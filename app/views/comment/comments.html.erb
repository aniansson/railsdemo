<%= form_for([@article.@article.comments.build]) do |f| %>
  <p>
      <%= f.label :email %>
  </p>
  <p>
    <%= f.label :body %><br>
    <%= f.text_area :body %>
  </p>
  <br>
  <p>
    <%= f.submit %>
  </p>
<% end %>
